rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All client-side access denied.
    // Backend uses Admin SDK (bypasses these rules).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
