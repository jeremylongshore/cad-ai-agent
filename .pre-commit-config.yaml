repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: detect-private-key
      - id: no-commit-to-branch
        args: ['--branch', 'main']

  - repo: local
    hooks:
      - id: forbid-env-files
        name: Forbid .env files
        entry: bash -c 'echo "Do not commit .env files!" && exit 1'
        language: system
        files: '^\.env$'
      - id: audit-harness-verify
        name: audit-harness verify (hash-pin tamper check)
        entry: bash scripts/audit-harness verify
        language: system
        pass_filenames: false
        always_run: true

      - id: audit-harness-verify
        name: audit-harness verify (hash-pinned policy)
        entry: scripts/audit-harness verify
        language: system
        pass_filenames: false
        stages: [pre-commit]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.5.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks

  - repo: https://github.com/alessandrojcm/commitlint-pre-commit-hook
    rev: v9.18.0
    hooks:
      - id: commitlint
        stages: [commit-msg]
        additional_dependencies: ['@commitlint/config-conventional']
